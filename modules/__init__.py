"""Modules - optional features that can be enabled/disabled.

Each module is self-contained with its own dependencies.
Modules don't import each other directly - they use EventBus and DI container.
"""
